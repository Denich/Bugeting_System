@if(Request.IsAuthenticated) {
    <text>Welcome <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("Вийти", "LogOff", "Account") ]</text>
}
else {
    @:[ @Html.ActionLink("Ввійти", "LogOn", "Account") ]
}
