@if(Request.IsAuthenticated) {
    <text >Welcome <strong>@User.Identity.Name</strong>!
    @Html.ActionLink("[Вийти]", "LogOff", "Account", null,  new { @class = "btn" }) </text>
}
else {
    @: @Html.ActionLink("Ввійти", "LogOn", "Account", null,  new { @class = "btn btn-inverse" }) 
}
